[package]
name = "guessing_game"
version = "0.1.0"
edition = "2021"

[dependencies]
rand = "^0.8.5"
leptos = { version = "0.6", features = ["csr"] }
wasm-bindgen = "0.2"
web-sys = { version = "0.3", features = ["Window", "Document", "HtmlElement"] }

[lib]
crate-type = ["cdylib"]