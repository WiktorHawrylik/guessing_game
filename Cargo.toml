[package]
name = "guessing_game"
version = "0.1.0"
edition = "2024"

[dependencies]
rand = "^0.8.5"

[target.'cfg(target_arch = "wasm32")'.dependencies]
leptos = { version = "0.6", features = ["csr"] }
